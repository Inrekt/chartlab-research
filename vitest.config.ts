import { defineConfig } from "vitest/config";

/**
 * Конфиг существует ради ОДНОЙ строки — исключения рабочих копий.
 *
 * Забытая git-worktree (`.claude/worktrees/...`) содержит полную копию
 * репозитория, и без этого исключения vitest подхватывал её тесты: прогон
 * показывал 796 «зелёных» вместо 398, и ровно половина из них проверяла
 * УСТАРЕВШИЙ код из копии. Это опаснее лишнего времени: копия проходит свои
 * тесты сама с собой и создаёт ложное спокойствие ровно тогда, когда в
 * настоящем коде что-то сломано.
 *
 * Ловится такое только счётом файлов — падений нет, всё «зелено».
 */
export default defineConfig({
  test: {
    exclude: [
      "**/node_modules/**",
      "**/dist/**",
      ".claude/worktrees/**",
      "**/graphify-out/**",
    ],
  },
});
