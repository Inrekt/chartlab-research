# Пре-регистрация семейства «ликвидити-магнит»

Дата фиксации: 2026-07-31. Записано ДО первого прогона — это условие,
без которого результат не считается (см. жёсткие правила плана).

## Экономическая гипотеза: кто платит и почему

Стратегия владельца, торгуемая им вручную с положительным результатом:
**шорт после жёсткой перекупленности, цель — ближайшее скопление
ликвидности, стоп — за всей ликвидностью с запасом.**

Носитель причины — не фигура на графике, а конкретный контрагент:
**перегруженные плечами лонги, которых обязан ликвидировать риск-движок
биржи.** Их выход с рынка не является добровольным и не зависит от их
мнения о цене: при касании уровня ликвидации позиция закрывается рыночным
ордером принудительно. Это структурный поток — тот же класс, что
ребалансировки индексов и хеджирование дилеров опционов, а не «паттерн».

Из этого следуют два проверяемых предсказания, и именно они, а не
прибыльность сама по себе, отличают гипотезу от подгонки:

1. **Направленность.** Цена чаще доходит до плотного скопления расчётных
   уровней ликвидации, чем до равноудалённой точки, где скопления нет.
2. **Условность.** Эффект должен быть сильнее там, где плечо реально
   перегружено — при высоком открытом интересе и вытянутом фандинге, — и
   слабее в спокойном режиме. Если эффект одинаков везде, это не
   ликвидационная механика, а что-то другое, и гипотеза не подтверждена,
   даже если деньги есть.

## Данные: что есть и чего нет — честно

- **Расчётная карта ликвидаций** (`src/core/liquidations/estimateHeatmap.ts`)
  — МОДЕЛЬ, не отчёт биржи: для каждого бара считается, где ликвидировались
  бы позиции, открытые на нём с плечами 10/25/50/100, с весом по объёму
  бара. Работает на всей истории корпуса (5 лет), потому что нужны только
  свечи. Упрощение (без поддерживающей маржи, фандинга, кросс/изолированной
  специфики) заявлено в самом коде. **Поиск идёт по этой модели.**
- **Реальные ликвидации Binance** (`scripts/collect-liquidations.ts`,
  крон каждые 10 минут) — настоящий поток `!forceOrder@arr`, но у Binance
  НЕТ исторической выгрузки: архив накапливается только вперёд, на диске
  сейчас 0 дней. Поэтому бэктест на нём невозможен по построению.
  **Роль реальных данных — подтверждение на живых сделках в инкубаторе, а
  не поиск.** Это ограничение записано заранее, чтобы потом не выдать
  модель за биржевые данные.

Риск, который отсюда следует и который нельзя замазать: если край найдётся,
он может оказаться краем МОДЕЛИ (артефакт формулы «цена входа × плечо»), а
не рынка. Проверка — п. 2 гипотезы (условность по OI/фандингу, данные уже
собираются с 31.07) плюс живой инкубатор на реальном потоке ликвидаций.

## Что нужно добавить в движок

Текущая грамматика этого выразить не может — отсюда и «невыразимо» в
разборе. Нужны:

1. Атом условия **«растянутость»**: |цена − SMA| / ATR ≥ порог, либо
   перцентиль этого отношения. (Сейчас есть только RSI/z-score по цене.)
2. Атом **«скопление ликвидности впереди»**: в расчётной карте есть корзина
   с плотностью ≥ порога на расстоянии от цены в пределах [min, max] ATR.
3. **Выходы от уровней**, а не от фиксированного R: цель — ближайшая плотная
   корзина по направлению сделки; стоп — за самой дальней плотной корзиной
   ПРОТИВ направления плюс запас в ATR. Это новый тип exit в
   `StrategyConfig` (сейчас только atr-стоп + R-цель + потолок баров).

Пункт 3 — самая существенная правка: он меняет тип стратегии, а не только
грамматику, и обязан пройти те же тесты паритета движка (бэктест и инкубатор
исполняют одинаково).

## Комбинаторика — намеренно малая

Порог растянутости 3 значения × порог плотности 3 × окно поиска кластера 2 ×
запас за стопом 3 × направление 2 × ТФ 2 = **216 вариаций**. Это на два
порядка меньше старого перебора: цена дефляции за новое семейство должна
оставаться низкой, иначе DSR съест его так же, как съел индикаторы.

## Критерий убийства (назван заранее)

Семейство считается неподтверждённым и закрывается, если выполнено любое:

- ни одна вариация не проходит разностную нуль-модель (t ≥ 2.6) — то есть
  «магнит» не лучше случайных входов с тем же расписанием;
- вариации проходят, но **эффект не зависит от плотности кластера** (тот же
  результат при пороге плотности, выбранном наугад) — значит работает
  растянутость сама по себе, а ликвидность ни при чём, и семейство надо
  переименовать и пере-пре-регистрировать как mean-reversion;
- проходит in-sample, но валится на срезе out-of-time (t < 1.3).

## Ожидание

Осторожное. Ручной результат владельца — свидетельство в пользу гипотезы, но
он не отделён от исполнения, выбора момента и управления позицией вручную.
Формализованная версия проверяет только правило. Расхождение между «работает
у человека» и «не работает в коде» — сам по себе полезный результат: он
означает, что край в исполнении, а не в правиле.

---

## ⚠️ Поправка (2026-08-09): что код измеряет на самом деле

Текст выше НЕ переписан и переписан не будет — пре-регистрация тем и ценна,
что её нельзя править задним числом. Ниже — поправка: проверка показала, что
раздел «Данные» и поле «кто платит» описывают не тот механизм, который
исполняется кодом. Расходятся документ и код; исправлять надо документ, потому
что код делает осмысленную вещь — просто другую.

### 1. Про `estimateHeatmap.ts`: файл существует, но не в этом репозитории

- В `chartlab-research` его **никогда не было**. По полной истории всех веток
  единственные файлы, когда-либо лежавшие в `src/core/liquidations/`, — это
  `clusterSeries.ts` и `clusterSeries.test.ts`. Удаления не было: искать
  «в каком коммите потеряли» нечего.
- Файл реально существует в **соседнем репозитории приложения**
  (`~/Desktop/chartlab`, там UI): `src/core/liquidations/estimateHeatmap.ts`
  плюс его тест и два слоя отрисовки. Это действительно та модель, что описана
  выше: тиры плеча 10/25/50/100, цена ликвидации `entry × (1 ∓ 1/leverage)`,
  вес по объёму бара, 40 корзин.
- Но исследовательский конвейер его **не импортирует ни в одной точке**. В
  `chartlab-research` имя `estimateHeatmap` встречается только в комментариях и
  в трёх документах — ни одного `import`.

Итог: фраза «**Поиск идёт по этой модели**» в разделе «Данные» неверна.
Поиск по расчётной карте ликвидаций не шёл никогда. Документ ссылался на
существующий файл из репозитория UI и молча предположил, что движок поиска
работает на нём.

### 2. Что на самом деле вычисляет атом `liquidity`

Цепочка: `evaluator.ts` (атом `kind: "liquidity"`) → `cachedLiquidityFeatures`
→ `src/core/liquidations/clusterSeries.ts`. Это **чисто ценовая геометрия из
OHLC**. Ни плеча, ни объёма, ни открытого интереса, ни фандинга, ни формулы
цены ликвидации в расчёте нет.

- **Уровень** = подтверждённый свинг-пивот: бар, строго превосходящий три
  соседних бара с каждой стороны (`PIVOT_WING = 3`). Узнаётся причинно — только
  через 3 бара после экстремума.
- **Вес** = 1 + число повторных подходов. Новый пивот сливается с уже живым
  уровнем, если цены расходятся не более чем на `MERGE_ATR = 0.25` ATR; цена
  уровня остаётся от более раннего пивота.
- **Снятие** = уровень исчезает на первом же баре, где `high ≥ цены` (сверху)
  или `low ≤ цены` (снизу), причём проверка идёт ДО чтения фич на том же баре.
- **Горизонт** = `HORIZON_ATR = 12` ATR; `near*` — ближайший живой уровень,
  `far*` — самый дальний в горизонте (за него ставится стоп).

**Важная тонкость, меняющая смысл слова «вес».** Снятие использует `≥`, а не
`>`. Значит повторный экстремум РОВНО на той же цене или выше уровень
**сносит**, а не усиливает: вес обнуляется, рождается новый уровень с весом 1.
Вес копится только тогда, когда каждый следующий почти-равный пивот строго
**ниже** сохранённой цены уровня (для стороны `above`) и попадает в те же
0.25 ATR. То есть `minWeight ≥ 2` отбирает не «равные максимумы» в буквальном
смысле, а слегка нисходящую серию почти-равных максимумов, ни разу точно не
дотянувшуюся до первого. Это подтверждено тестами `clusterSeries.test.ts`
(«равные максимумы копят вес» использует 109 → 108.97 → 108.95; «максимум ВЫШЕ
прежнего снимает уровень»).

Отсюда: пункт 2 раздела «Что нужно добавить в движок» («в расчётной карте есть
корзина с плотностью ≥ порога») реализован не так, как записан. «Плотность
кластера» в коде — это **число подходов к неснятому пивот-экстремуму**, а не
плотность расчётных ликвидаций.

### 3. Кто платит — исправленная формулировка

| | записано в пре-регистрации | что меряет код |
|---|---|---|
| контрагент | перегруженные плечом лонги | трейдеры с покоящимися заявками за экстремумом |
| принуждение | риск-движок биржи, недобровольно | стоп срабатывает сам, но заявку поставил трейдер |
| координата | из цены входа × плечо, трейдер её не выбирал | трейдер выбрал её сам, по видимой геометрии графика |
| данные для проверки | OI, фандинг, поток ликвидаций | только свечи |

Это **разные платящие стороны**, а не две формулировки одной. Механизм от
поправки не становится пустым — покоящиеся стопы за многократно
протестированным экстремумом это по-прежнему пул вынужденных продавцов,
и это законная причинная история. Но она другая, и проверяется другими
данными.

Честная формулировка «кто платит» для того, что исполняется:

> Трейдеры, поставившие стоп-заявки за неснятым свинг-экстремумом, к которому
> цена подходила несколько раз, не пробив его. Чем больше подходов, тем больше
> заявок там накопилось. Их срабатывание — самоусиливающийся поток в сторону
> уровня; координата известна заранее, потому что её видно на графике всем.

Тот же вывод независимо получен при разборе чернового семейства «равные
экстремумы» — `DRAFT-family-equal-highs.md`, раздел 6.3. Вывод там же:
не заводить новое имя под уже измеряемую величину, а исправить `whoPays` у
существующего семейства (`liquidity_magnet` v2).

### 4. Что это меняет в уже полученных результатах (648 комбинаций)

В журнале все **648 испытаний в состоянии REJECTED**; 648 оценок на экране
`halving_16`, 38 дошли до `halving_128`, 6 до `gate_activity`. Ни одно не
провалидировано, в инкубатор ничего не попало. Ложных промоушенов поправка не
вскрывает — но интерпретацию меняет, и в четырёх местах.

**а) Фальсифицировано не то, что написано.** Отрицательный результат закрывает
правило «растянутость от SMA + неснятый повторный пивот-экстремум впереди».
Гипотеза ликвидационного магнита им **не проверена и не закрыта** — по ней не
было ни одного испытания. Считать эти 648 свидетельством против ликвидационной
механики нельзя; если её захотят проверить, это отдельное семейство на
отдельной пре-регистрации, и данных для него (историческая карта ликвидаций в
поисковом репозитории) сегодня нет.

**б) Предсказание 2 не проверялось вообще.** «Условность» — эффект сильнее при
перегруженном плече (высокий OI, вытянутый фандинг) — названа в документе
главным, что отличает гипотезу от подгонки. У сетапов семейства стоит
`fixedRule: true`, то есть комбинации фильтров для него **не перебираются
вовсе**, а само правило состоит ровно из двух атомов: `stretch` и `liquidity`.
Ни в одном из 648 спеков нет ни фандинга, ни OI. Отличающее предсказание было
структурно невыполнимо на всём прогоне.

**в) Критерий убийства № 2 частично отвечен — и ответ отрицательный.** Ось
`minWeight ∈ {1,2,3}` и есть заявленная «ось плотности». По журналу
(`halving_16`, средняя нетто-ожидаемость и среднее число сделок):

| minWeight | испытаний | ср. нетто-ожидаемость | ср. сделок |
|---|---|---|---|
| 1 | 216 | −0.0931 | 2496 |
| 2 | 216 | −0.0930 | 418 |
| 3 | 216 | −0.1517 | 43 |

Дозо-ответа нет: между w1 и w2 разницы нет вообще, на w3 хуже при обвале числа
сделок в 58 раз. «Плотность» не добавляет ничего сверх растянутости. Формально
критерий № 2 записан для случая «вариации проходят, но…», а они не прошли —
умерли на первом же экране. Поэтому это не срабатывание критерия, а более
слабое, но однонаправленное свидетельство: магнитная часть правила не несёт
сигнала.

**г) Сами числа поправка не портит.** Заглядывания вперёд нет: `clusterSeries`
причинен по построению, и причинность прибита тестом (префикс серии равен
полной серии на общем участке). Меняется ярлык механизма, а не статистика.
Отдельная, не связанная с этой поправкой оговорка: прогон выполнен на
`gateVersion=3`, то есть до правки исполнения гэпа через стоп от 2026-08-08.

**д) Ошибка продублирована в коде и утекла в журнал.** Поле `whoPays` у
`LIQUIDITY_SETUPS` в `researcher/grammar.ts` повторяет ту же формулировку про
«перегруженных плечом лонгов». Значит у всех 648 записей в журнале механизм
записан неверно. Это правится в коде отдельным изменением; настоящий документ
кода не касается.

### 5. Что поправка НЕ меняет

Размер пространства (648 после расширения на добор), форма критериев убийства,
статус документа как пре-регистрации и запрет на его переписывание. Поправка
исправляет только описание источника данных и платящей стороны — то есть
приводит документ в соответствие с тем, что полтора месяца исполнялось.
